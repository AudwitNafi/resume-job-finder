# Project settings
DOMAIN=localhost
FRONTEND_HOST=http://localhost:3000

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="Full Stack FastAPI Project"
STACK_NAME=full-stack-fastapi-project

# Backend
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:3000,https://localhost,https://localhost:3000,http://localhost.tiangolo.com"
SECRET_KEY=your-secret-key
FIRST_SUPERUSER=your-admin@example.com
FIRST_SUPERUSER_PASSWORD=your-secure-password

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-db-password

# Docker images (replace with your registry/image names if needed)
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend
